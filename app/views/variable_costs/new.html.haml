%h2 変動費登録
.col-12
  = form_with model: @variableCost, url: variable_costs_path, local: true do |form|
    - @variableCost.collection.each do |variable_cost|
      = fields_for 'variable_cost[]', variable_cost do |f|
        .form-group
          = f.label :name, '商品名'
          = f.text_field :name, class: 'form-control'
        .form-group
          = f.label :content, '用途'
          = f.text_area :content, class: 'form-control', rows:5
    = form.submit '登録'
